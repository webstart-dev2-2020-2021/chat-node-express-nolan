<main class="col main pt-5 mt-3 h-100 overflow-auto">
	{{#if users}}
      	{{#each users}}
      	<div class="note note-primary w-100">{{id}}: {{email}} {{password}} {{is_admin}}</div>
      	{{/each}}
    {{else}}
    <p>No users found</p>
    {{/if}}
</main>